package view_controller;

import javafx.fxml.FXML;
import javafx.fxml.Initializable;
import javafx.scene.text.Text;

import java.net.URL;
import java.util.ResourceBundle;

// when creating a new customer
// Grab values from fields
// Do field validation
//
// first look at country table and see if that country exists
// -> if that country doesn't exist create that country
// then the same for city
// then the same for address
// then the same for customer
public class AppointmentAddNewController implements Initializable {







    @Override
    public void initialize(URL location, ResourceBundle resources) {
    }
}
